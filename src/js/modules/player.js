/**
 * Video Player Module
 * Minimal integration for YouTube Embed
 */

export function initVideoPlayer() {
    const container = document.getElementById('video-container');
    if (container) {
        console.log('🎥 Video Player Tab Initialized');
        // Future interactions with the iframe can be added here if needed
    }
}
